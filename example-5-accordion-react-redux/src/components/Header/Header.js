import React, {PropTypes} from 'react';


const Header = () => {

    return (
        <header className="App-header">
            <h1>Accordion</h1>
        </header>
    );
};

export default Header;
